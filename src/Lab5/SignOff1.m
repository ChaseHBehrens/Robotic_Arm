%Testing camera SignOff1

%camera = Camera();

%camera.calibrate(); % Calibrating camera

%camera.getImage(); % obtaining camera view

%disp(camera.getCameraPose()); % observing camera position in regards to the plane